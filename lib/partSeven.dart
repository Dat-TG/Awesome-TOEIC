import 'package:flutter/material.dart';
import 'main.dart';

class PartSeven extends StatelessWidget {
  const PartSeven({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Đọc hiểu đoạn văn"),
          backgroundColor: colorApp,
          centerTitle: true,
        ),
        body: Text("Part Seven"));
  }
}

